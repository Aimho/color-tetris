{
  "name": "color-tetris",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "test": "node --test",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  },
  "dependencies": {
    "firebase": "^12.16.0"
  }
}
